pip install Flask
pip install json
pip install urllib
pip install datetime
pip install -U python-dotenv

(install virtual environment:
python3 -m venv venv
)

. venv/bin/activate
export FLASK_APP=hermes.py
flask run
